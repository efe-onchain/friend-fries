{
  "name": "friendfries",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ friendfries",
    "create-local": "graph create --node http://localhost:8020/ friendfries",
    "remove-local": "graph remove --node http://localhost:8020/ friendfries",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 friendfries",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.76.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "@graphprotocol/client-cli": "^3.0.3",
    "matchstick-as": "0.5.0"
  }
}
